node_modules/
dist/
out/
coverage/
.turbo/

# TypeScript incremental (composite projects)
*.tsbuildinfo

# Vite / Electron generated (see electron.vite.config.ts)
src/renderer/public/onnx-wasm/

# Swift Package Manager — never commit build trees
native/**/.build/

# Local macOS shell experiment bundle (not a shipped artifact)
build/JarvizShell.app/

# OS / editor
.DS_Store
thumbs.db
*.log

# Environment and credentials
.env
.env.*
*.env
credentials.json
*.pem
*.key
.credentials
